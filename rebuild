docker rm -f $(docker ps -q --filter name=srsRTMP)
docker build -t srs-rtmp:0.1 .
docker run --name srsRTMP -p 8085:1935 --restart unless-stopped srs-rtmp:0.1
docker exec -it $(docker ps -q --filter name=srsRTMP) /bin/bash
